export default function PageLoader() {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-white/80 z-50">
      <div className="w-12 h-12 border-4 border-background-primary border-t-transparent rounded-full animate-spin"></div>
    </div>
  );
}
